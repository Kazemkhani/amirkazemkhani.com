# Analytics — the Plausible script is injected ONLY when this is set.
# Leave unset for a fully inert, zero-request site.
VITE_PLAUSIBLE_DOMAIN=

# Booking — the "Book a call" button renders ONLY when this is set.
VITE_CAL_URL=
